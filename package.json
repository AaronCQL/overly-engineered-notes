{
  "name": "aaroncql-cs3219-otot-b",
  "description": "Do not install any packages here. This should only be used as a convenient way to run scripts in the root.",
  "author": "Aaron Choo",
  "license": "MIT",
  "private": true,
  "scripts": {
    "install": "yarn --cwd ./server",
    "dev": "yarn --cwd ./server dev"
  }
}
